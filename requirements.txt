# ─── ATenIR-Selective-Recomputation 依赖 ─────────────────────────

# 安装示例（清华源）:
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
# PyTorch ≥ 2.6 + CUDA 12.4 — 通过 conda / Docker 基础镜像安装，不在此指定
# 安装: pip install -r requirements.txt

# 模型
transformers==4.35.2
huggingface_hub>=0.16.4,<1.0
tokenizers>=0.14,<0.19

# 图算法（min-cut 分区器依赖）
networkx>=3.0

# 输出与可视化
matplotlib>=3.7
tabulate>=0.9

# 开发 / 测试
pytest>=7.0
