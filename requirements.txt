# Runtime and thesis-reproduction dependencies.
#
# Recommended:
#   conda create -n torch2.6-gpu python=3.10 -y
#   conda activate torch2.6-gpu
#   pip install -r requirements.txt
#
# The extra index provides CUDA 12.4 PyTorch wheels. If your machine needs a
# different CUDA build, replace the torch install source according to the
# official PyTorch command and keep the remaining package pins unchanged.

--extra-index-url https://download.pytorch.org/whl/cu124

# PyTorch / compiler stack
torch==2.6.0
triton>=3.2,<3.4

# 模型
transformers==4.35.2
huggingface_hub>=0.16.4,<1.0
tokenizers>=0.14,<0.19

# 数值与图表
numpy>=1.24
matplotlib>=3.7
tabulate>=0.9

# 图算法（min-cut 分区器依赖）
networkx>=3.0

# 开发 / 测试
pytest>=7.0
