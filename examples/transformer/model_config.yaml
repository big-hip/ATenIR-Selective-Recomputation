version: 1

model:
  # 仅放模型结构相关超参数；运行参数放 input.yaml
  src_vocab_size: 5000
  tgt_vocab_size: 5000
  d_model: 512
  num_heads: 8
  num_layers: 6
  d_ff: 2048
  max_seq_length: 100
  dropout: 0.1
  padding_idx: 0
