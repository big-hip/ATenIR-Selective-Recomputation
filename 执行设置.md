执行环境
user/tangchengxiang/miniconda3/envs/torch2.6-gpu
你执行一次capture,我想看一下输出

python "/user/tangchengxiang/毕设/ATenIR-Selective-Recomputation/examples/transformer/capture.py"

这个竟然能对比实际执行的图和meta图的区别，太恐怖了，我要搞明白是怎么做到的

# Context

当前目标不是立刻下结论“谁更好”，而是先把项目方向重新校准：

1. **优先正确使用和复现 PyTorch 自带重计算能力**，弄清楚 checkpoint / SAC / graph-level remat 到底该怎么接、怎么跑、怎么验证；
2. **在此基础上增添和完善本项目的重计算策略**，并验证这些策略至少要相对“无重计算”产生真实有效果，真正体现重计算技术的意义；
3. **建立一个尽可能准确的静态仿真体系**，对模型进行高精度静态内存/开销估算，并与真实执行结果逐项对照，尽可能缩小 static/runtime gap；
4. 策略之间“谁更优”的横向对比可以做，但它是次级目标，前提是：
   - PyTorch built-in 路径被正确接入；
   - ATenIR 路径本身有效；
   - 静态仿真数值足够可信。

这一步之所以必要，是因为最近的现象已经暴露出两个更本质的问题：
- 直接把 `partition_fn=min_cut_rematerialization_partition` 塞进一个 backend，**不能自动代表 PyTorch 面向用户的完整重计算方法**；
- 当前 static 数值与 runtime 表现存在明显偏差，说明项目的静态仿真和实际执行之间还缺少足够强的校准链路。

因此，当前最重要的不是直接下结论“PyTorch min-cut 没效果”，而是先把以下四件事分清：
1. **PyTorch 用户级方法**怎么正确使用、怎样算复现成功；
2. **PyTorch 内部 partitioner**在整条 compile 链路里扮演什么角色；
3. 本仓库 Transformer 例子里，**哪些对比是语义对齐的，哪些不是**；
4. **静态仿真到底应该统计什么，怎样才能尽可能逼近真实执行**。

这意味着后续实现和实验的排序应该是：
- 先把 PyTorch built-in recomputation 路径接对、跑通、验证正确；
- 再看 ATenIR 策略能否稳定带来相对 no-recompute 的收益；
- 最后再做策略间优劣比较。

# Recommended approach

## 执行原则（必须遵守）

- **能复用的复用**：已有 core / analysis / benchmark 基础能力优先复用，避免重复造轮子。
- **该大改就大改**：如果现有 examples/transformer 或 benchmark/capture 框架结构不合理，就直接重构，不为了少改而保留坏结构。
- **以正确性、合理性、效果为第一优先**：
  - 正确性优先于兼容旧实验脚本；
  - 合理性优先于局部补丁式修补；
  - 效果优先于形式上的“已经接入”。
- **不为了“少改”保留不合理结构**：后续如果发现某层职责混乱、边界错误、数据流不清晰，应直接调整到更合理的布局。

## 0. 重新定义项目优先级

- 先把结构做对，再把实验跑对，再谈结果比较。
- 允许对 `examples/transformer/benchmark_flow/*` 做较大重构；`aten_recompute/core/*` 和 `analysis/*` 优先复用，只有在核心逻辑或统计逻辑本身不合理时才修改。
- 所有新增实现都要服务于：
  1. 正确复现 PyTorch built-in recomputation；
  2. 验证相对 no-recompute 的有效性；
  3. 建立高精度 static/runtime 对账。

## 0. 重新定义项目优先级

后续工作按以下优先级推进：

### Priority A — 正确接入与复现 PyTorch built-in recomputation
目标不是“证明它不行”，而是：
- 把 PyTorch 官方/用户级推荐路径真正接入 Transformer；
- 在本仓库里形成可重复、可验证、可 profiling 的 built-in baseline；
- 明确每条 built-in 路径属于哪种语义（region / graph / compile internal）。

### Priority B — 验证重计算技术本身是否有效
对每条方法，最先要回答的不是“谁最好”，而是：
- 相对 no-recompute，是否真的降低了目标指标；
- 至少在某些配置上，是否体现出重计算带来的真实收益；
- 若没有收益，问题出在语义、调度、实现还是统计口径。

### Priority C — 建立高精度静态仿真
静态仿真不应只是方向性工具，而应尽量成为：
- 可解释；
- 可校准；
- 能与 runtime 逐项对账；
- 对不同策略给出尽可能精确、稳定的估计。

### Priority D — 再做策略优劣比较
只有在 A/B/C 都站住后，才进入“ATenIR vs PyTorch built-in / strategy 6 vs 7”的主结论阶段。

## 1. 先把比较对象按“语义层级”拆开，而不是混成一个 PyTorch baseline

后续实验必须分成两类，不要再把它们混成一个“PyTorch 自带重计算”数字：

### A. 用户级 / region-based 方法
这类方法的核心语义是：用户显式定义一个 checkpoint region，反向时 replay 这个 region。

包括：
- `torch.utils.checkpoint.checkpoint`
- selective activation checkpoint / SAC：
  - `CheckpointPolicy`
  - `SelectiveCheckpointContext`
  - `create_selective_checkpoint_contexts(...)`
- 若当前安装版本可用，再额外评估 activation memory budget 路径

### B. 编译期 / graph-level 方法
这类方法的核心语义是：先形成 joint graph，再在图级别决定 save / recompute。

包括：
- PyTorch compile 内部的 `min_cut_rematerialization_partition`
- 本仓库的 ATenIR 路线（当前重点是 `strategy 7`）

后续报告必须分别给出：
- **region vs region** 对比（checkpoint / SAC / memory-budget if available）
- **graph vs graph** 对比（PyTorch built-in graph remat vs ATenIR strategy 7）

不能再把 “裸 partitioner 调用” 直接当成 “PyTorch 官方完整方法”。

## 2. 先确认 PyTorch 官方推荐使用链路，而不是只看单个 internal API

基于已调研到的 site-packages 源码，后续实现和解释都应围绕以下调用链理解：

### 用户入口
- `torch.utils.checkpoint.checkpoint(..., use_reentrant=False, context_fn=...)`
- `create_selective_checkpoint_contexts(policy_fn_or_list)`
- `CheckpointPolicy`

### compile 下的内部接线
- Dynamo 将 checkpoint 转成 higher-order op
- `tag_activation_checkpoint` / `wrap_activation_checkpoint` 给 FX 节点打 `recompute` / `ac_graph_id` 等元信息
- AOTAutograd / Inductor 最终再调用 `min_cut_rematerialization_partition(...)`

也就是说，**PyTorch 真正的 built-in method = 用户 API + compiler wiring + internal partitioner**。

因此：
- `partition_fn=min_cut_rematerialization_partition` 这种实验可以保留，
- 但它只能被标记为：
  - “partitioner-only diagnostic”
  - 不能当作主结论里的 “PyTorch 自带 min-cut 方法”

## 3. 明确本仓库当前有哪些路径已经存在，哪些还没有

优先复用现有能力，避免误以为仓库里已经接了 PyTorch 内置方法。

### 已存在 / 应复用
- `examples/transformer/meta_pipeline.py`
  - `inject_transformer_layer_tags()`
  - `capture_static_with_retry()`
- `examples/transformer/model_loader.py`
  - `build_transformer_from_map()`
- `examples/transformer/benchmark_flow/train_phase.py`
  - 构建模型、输入、criterion、optimizer 的现成组织方式
- `aten_recompute/analysis/static.py`
  - `StaticEstimator`
- `aten_recompute/analysis/profiler.py`
  - `MemoryProfiler.profile_step()`
- `aten_recompute/core/partition.py`
  - `_apply_strategy()` 已实现 strategy 6/7 调度
- `aten_recompute/core/min_cut.py`
  - `solve_min_cut_recompute(...)`
- `aten_recompute/utils/checkpoint.py`
  - 普通 PyTorch layer-checkpoint 包装器

### 目前 Transformer 里缺失 / 需要补接的
- PyTorch SAC / selective checkpoint 在 Transformer 上的真实 runtime 路径
- PyTorch built-in graph-level remat 在 Transformer 上的“方法级”接法
- 将这些 built-in 方法纳入 `examples/transformer/benchmark_flow/runtime_phase.py` 的统一 profiling

### 已有先例但只在 SimpleMLP 中存在
- `examples/simple_mlp/capture.py`
  - `_SACBackend`
  - `min_cut_rematerialization_partition`
  - `create_selective_checkpoint_contexts(...)`
  - `CheckpointPolicy`

这个文件应作为 Transformer 侧接入 PyTorch built-in recomputation 的参考模板，而不是直接拿它的结果外推到 Transformer。

## 4. 先解决“什么算有效”，再谈“谁更好”

后续每条方法都必须先过“有效性”这一关。

### 有效性的最低标准
相对 no-recompute，至少在某些配置上要满足之一：
- runtime 峰值显存下降；
- static / runtime 至少同向改善；
- 若时间略增，也要能换来明确的内存收益；
- 或者在 correctness/稳定性上证明它是可用的标准实现基线。

### 对 built-in PyTorch 的要求
- 不是只要“跑通”就算复现成功；
- 要形成一个在 Transformer 上真正可测、可重复、可解释的 baseline；
- 并能回答：相对 no-recompute，它是否真的体现了重计算技术的效果。

### 对 ATenIR 的要求
- 不是只看 strategy 之间谁高谁低；
- 首先要证明：至少有一类策略能够稳定体现真实收益；
- 若所有策略都不相对 no-recompute 改善，则应优先怀疑：
  - 执行语义
  - 调度/释放边界
  - 静态估计口径
  - 而不是继续堆更多策略编号。

## 5. 把静态仿真作为一条独立主线建设，而不是附属指标

静态仿真现在需要从“方向性估计”升级为“尽可能精确的可校准系统”。

### 静态仿真的目标
后续 static analysis 应尽量做到：
- 对参数、梯度、优化器、saved activations、重算临时量分项统计；
- 明确 FW peak / BW peak / optimizer phase peak；
- 尽量贴近真实执行中的主导内存来源；
- 可和 runtime profiler 做逐项对账。

### 数值精度要求
后续统计必须尽量减少“粗略估计”：
- 优先使用 graph/meta 中真实可得的 tensor shape / dtype / bytes；
- 对每一项数值尽量保留字节级统计，再在展示层换算 GB/MB；
- 明确哪些量是精确统计，哪些量是公式推导，哪些量是不可见开销；
- 结果输出中标注误差来源，而不是把所有 static 数值都当成同一可信度。

### 建议的 static/runtime 对账维度
至少分开对账：
- resident memory：params / grads / optimizer
- saved activations
- recompute-induced temporaries（能见部分）
- FW phase peak
- BW phase peak
- overall estimated peak vs measured peak
- static/runtime gap 绝对值与比例

### 对 static 结论的要求
后续 static 结果应回答的不只是“哪个更低”，而是：
- 为什么低；
- 低在哪一项；
- 哪些项在 runtime 里兑现了；
- 哪些项没有兑现；
- 偏差主要来自模型语义、编译调度、还是统计盲区。

## 6. 先解决“什么算公平对比”，再写代码

后续对比只允许出现以下两类公平实验：

### 实验组 A：用户级方法对比（region semantics）
同一 Transformer，同一 batch / seq / optimizer / step 定义，对比：
- eager baseline（无 checkpoint）
- eager + 普通 checkpoint
- eager + SAC
- eager + memory budget（若当前版本存在用户级可用入口）

这一组首先回答的是：
- PyTorch built-in 重计算在 Transformer 上有没有真实效果；
- 若有效，它的收益边界和代价是什么。

### 实验组 B：图级方法对比（graph semantics）
同一 Transformer，同一 compiled workload，对比：
- compiled baseline（无 remat）
- PyTorch built-in graph remat 路径
- ATenIR `strategy 7`

这一组首先回答的是：
- graph-level recomputation 在 compile-mode 下是否相对 no-recompute 有效果；
- 然后才比较 PyTorch graph 路线与 ATenIR 的差异。

### 明确不能直接拿来作为主结论的对比
- “裸 `min_cut_rematerialization_partition` backend” vs ATenIR
- eager checkpoint timing vs compiled ATenIR timing
- 公式推导的 `pytorch_ckpt` static 结果 vs runtime peak 结果
- 当前 repo 中 `compiled_transformer` vs 自定义 `autograd.Function` wrapper 的 timing 差异

## 7. 先把 dropout / RNG 作为一级风险处理

调研已经确认：dropout / RNG 不是小细节，而是 correctness 前提。

### PyTorch checkpoint/SAC
- `torch.utils.checkpoint` 本身有 RNG state 保存/恢复逻辑
- selective checkpoint 的 `context_fn` 在 compile 下还有额外约束
- 在带随机性的 region 内，不能默认假设行为与普通 eager 完全等价，必须验证

### ATenIR strategy 7
- 本仓库已经在 `aten_recompute/core/min_cut.py` 里：
  - ban 随机 op
  - ban 计算密集 op
  - 修复 attention tuple/getitem sibling consistency
- 这说明项目作者已经意识到：random / attention output mixing 会直接影响梯度正确性

因此后续实验建议固定分两阶段：

#### 阶段一：语义验证阶段
- 在 dropout 关闭或 RNG 可控条件下对比
- 先验证 loss / grad / optimizer step 是否一致

#### 阶段二：真实训练阶段
- 再决定是否打开 dropout 做 memory/runtime study
- 若 repo 路线无法保证 dropout replay 语义一致，则必须在结论中明确说明

## 8. 明确 whole-graph 与 checkpoint-region 的差异，避免误用结论

后续文档和实验报告里必须明确写清：

- checkpoint / SAC：本质上是**region replay**
- `min_cut_rematerialization_partition`：本质上是**joint-graph partitioning**
- 本仓库 ATenIR strategy 7：本质上是**在 FW→BW boundary saved values 上做 graph-level selective recomputation**

所以：
- “ATenIR 比 checkpoint 更省显存” 这种话不能直接说，除非 replay scope 和比较语义已经对齐
- 更稳妥的写法应该是：
  - “在 graph-level rematerialization 语义下，ATenIR 与 PyTorch built-in partitioner 的表现对比”
  - “在 region checkpoint 语义下，PyTorch 内置方法的表现对比”

## 9. 推荐先做一张‘语义地图 + 指标地图’，再进入实现

开始编码前，先把待比较方法整理成一个方法矩阵：
- method name
- semantic family（region / graph）
- execution mode（eager / compiled）
- user-facing API / internal API
- RNG / dropout handling
- 当前 repo 是否已接线
- 是否适合作为主结论 baseline

同时再整理一张指标矩阵：
- 哪些指标是字节级精确统计
- 哪些指标是公式推导
- 哪些指标是 runtime 实测
- 哪些指标存在已知盲区（workspace / allocator / fusion temporaries 等）

这两张图会直接决定后续 benchmark harness 和 static simulator 应该如何组织。

# Critical files to modify

后续真正实现时，优先会动这些文件：

- `examples/transformer/benchmark_flow/runtime_phase.py`
  - 把 PyTorch built-in 方法纳入统一 runtime profiling
- `examples/transformer/benchmark_flow/runner.py`
  - 保持薄封装，接统一 orchestrator
- `examples/transformer/benchmark_flow/static_phase.py`
  - 区分“图捕获静态结果”和“checkpoint 公式推导结果”，并细化对账项
- `examples/transformer/benchmark.py`
  - 入口保持简单
- `examples/transformer/input.yaml`
  - 若需要方法矩阵配置，可最小扩展
- `aten_recompute/analysis/static.py`
  - 提升 static 统计精度与分项可解释性
- `aten_recompute/analysis/profiler.py`
  - 增强 static/runtime 对账输出

若需要参考现有 PyTorch built-in 接法，应重点复用：
- `examples/simple_mlp/capture.py`

# Existing functions/utilities to reuse

- `examples/transformer/meta_pipeline.py`
  - `inject_transformer_layer_tags()`
  - `capture_static_with_retry()`
- `examples/transformer/model_loader.py`
  - `build_transformer_from_map()`
- `examples/transformer/benchmark_flow/train_phase.py`
  - Transformer 训练步骤的公共组织方式
- `aten_recompute/analysis/static.py`
  - `StaticEstimator`
- `aten_recompute/analysis/profiler.py`
  - `MemoryProfiler.profile_step()`
- `aten_recompute/utils/checkpoint.py`
  - `apply_activation_checkpoint()`
- `examples/simple_mlp/capture.py`
  - `_SACBackend`
  - `partition_fn=min_cut_rematerialization_partition`
  - `create_selective_checkpoint_contexts(...)`
- site-packages PyTorch 源码（只作语义依据）
  - `torch/utils/checkpoint.py`
  - `torch/_higher_order_ops/wrap.py`
  - `torch/_dynamo/variables/higher_order_ops.py`
  - `torch/_functorch/partitioners.py`
  - `torch/_functorch/config.py`

# Verification

## Research verification
在真正实现前，应先确认以下问题都已经回答清楚：
- PyTorch built-in recomputation 的**用户入口**是什么？
- `min_cut_rematerialization_partition` 在整条 PyTorch compile 链路里扮演什么角色？
- 当前 Transformer 例子里，哪些 built-in 方法还没有接入？
- dropout / RNG replay 在各条路径上的语义风险是什么？
- static 仿真当前哪些项是精确统计，哪些项仍是近似？

## Implementation verification
后续开始实现后，应验证：
- `examples/transformer/benchmark.py` 能同时跑通 region-family 与 graph-family 的对照
- 每种方法的 execution mode 被正确标记（eager / compiled）
- PyTorch built-in 方法不再只停留在 static 公式推导，而是有真实 runtime 数据
- static report 能输出更细粒度的分项统计与 static/runtime gap

## Correctness verification
在开始比较 memory/runtime 之前，必须先验证：
- forward loss 接近
- 关键参数梯度接近
- optimizer step 更新接近
- 若 dropout 开启，RNG 语义已被明确控制或明确标注为非严格 correctness 对比

## Effectiveness verification
对每类方法都要先检查：
- 相对 no-recompute 是否至少在某个目标上产生正向收益；
- 若没有收益，是否能定位到是语义不匹配、调度限制，还是统计误差；
- 不能在“相对 no-recompute 都无效”的前提下，直接讨论策略之间谁更好。

## Interpretation check
最终结论必须避免以下错误表述：
- “PyTorch min-cut 没效果”
- “partition_fn=min_cut_rematerialization_partition 就等于 PyTorch 官方完整方案”
- “checkpoint-region 与 graph-level remat 可以直接视为同一种方法”
- “static 数字低就等于真实运行更省显存”

更稳妥的目标结论应是：
- PyTorch **用户级方法**在 Transformer 上是否被正确复现，以及相对 no-recompute 是否有效；
- PyTorch **图级 partitioner**在 compile-mode 下的表现如何；
- 本仓库 ATenIR strategy 7 是否至少在某些配置上真正体现了重计算技术效果；
- static simulator 对 runtime 的逼近程度如何，误差主要来自哪里。

## Suggested next execution step
开始实现之前，先基于上述语义地图确定一个**最终方法矩阵**和一个**统计指标矩阵**，再进入代码阶段。方法矩阵至少包含：
- eager baseline
- eager checkpoint
- eager SAC
- compiled baseline
- PyTorch built-in graph remat
- ATenIR strategy 7

指标矩阵至少包含：
- params / grads / optimizer bytes
- saved activation bytes
- FW peak / BW peak / overall estimated peak
- runtime measured peak
- static/runtime gap

只有这两张矩阵定清楚后，后续 benchmark 与 static simulator 的结果才值得解释。

## Phased execution order
后续真正执行时，按以下顺序推进：
1. 接对并验证 PyTorch built-in recomputation；
2. 验证每条方法相对 no-recompute 的有效性；
3. 提升 static simulator 精度并做 runtime 对账；
4. 最后再进入策略优劣比较。

## Key outcome target
最终要得到的不是一组零散 benchmark，而是：
- 一个被正确复现的 PyTorch recomputation baseline；
- 一个能体现 ATenIR 路线真实效果的 benchmark harness；
- 一个尽可能精确、并经过 runtime 校准的 static simulation pipeline。

只有做到这三点，后续关于 strategy 6/7、PyTorch built-in、ATenIR min-cut 的比较才有意义。

## 10. 当前真实项目结构与建议的文件布局方案

### 10.1 当前工作区真实结构（以后续实现应依赖的路径为准）

#### 核心算法层
- `aten_recompute/core/compiler.py`
  - `CompilerBackend`，ATenIR graph-level recomputation 的 compile backend 入口
- `aten_recompute/core/partition.py`
  - strategy 0/1/6/7 的 graph partition 主逻辑
- `aten_recompute/core/min_cut.py`
  - strategy 7 的 min-cut 选择逻辑
- `aten_recompute/core/strategy.py`
  - 策略配置解析、校验、描述
- `aten_recompute/core/tag.py`
  - `inject_layer_tags()` / `mark_layer()`

#### 分析与统计层
- `aten_recompute/analysis/static.py`
  - `StaticEstimator`，静态峰值仿真主入口
- `aten_recompute/analysis/profiler.py`
  - `MemoryProfiler`，runtime profiling 主入口
- `aten_recompute/analysis/flops.py`
  - FLOPs / Roofline 估算
- `aten_recompute/analysis/_activations.py`
  - FW→BW 边界激活与 bytes 统计
- `aten_recompute/utils/graph_utils.py`
  - boundary / structure 对照与 graph summary
- `aten_recompute/utils/checkpoint.py`
  - 普通 PyTorch checkpoint 包装器

#### Transformer 主实验层
- `examples/transformer/main.py`
  - 统一主入口，按 YAML task 分发
- `examples/transformer/input.yaml`
  - 主配置入口
- `examples/transformer/config_strict.py`
  - 配置校验与 env 注入
- `examples/transformer/model.py`
  - Transformer 定义
- `examples/transformer/model_loader.py`
  - clean model 构建
- `examples/transformer/meta_pipeline.py`
  - shared capture/compare helper

#### Transformer capture 子框架
- `examples/transformer/capture.py`
- `examples/transformer/capture_flow/config.py`
- `examples/transformer/capture_flow/capture_phase.py`
- `examples/transformer/capture_flow/analysis_phase.py`
- `examples/transformer/capture_flow/runner.py`

职责：图捕获、meta/runtime graph 语义与边界对照、验证型实验。

#### Transformer benchmark 子框架
- `examples/transformer/benchmark.py`
- `examples/transformer/benchmark_flow/config.py`
- `examples/transformer/benchmark_flow/train_phase.py`
- `examples/transformer/benchmark_flow/static_phase.py`
- `examples/transformer/benchmark_flow/runtime_phase.py`
- `examples/transformer/benchmark_flow/runner.py`
- `examples/transformer/benchmark_flow/strategy.py`

职责：训练侧 benchmark、static/runtime profiling、多方法对照输出。

#### PyTorch built-in 参考样例
- `examples/simple_mlp/capture.py`
- `examples/simple_mlp/model.py`

职责：
- 提供 `partition_fn=min_cut_rematerialization_partition` 接线示例
- 提供 `create_selective_checkpoint_contexts(...)` / `CheckpointPolicy` / SAC 接线示例

### 10.2 建议继续沿用的框架边界

后续不建议再新建一套并行框架，而是沿用当前 `examples/transformer` 的布局，把职责进一步稳定成四层：

#### Layer 1 — 输入与实验编排层
位置：
- `examples/transformer/main.py`
- `examples/transformer/input.yaml`
- `examples/transformer/config_strict.py`

职责：
- 唯一入口
- task 选择（capture / benchmark / 未来可扩 verify）
- 方法矩阵和实验参数装配
- env 注入与配置合法性校验

#### Layer 2 — 模型与方法装配层
位置：
- `examples/transformer/model.py`
- `examples/transformer/model_loader.py`
- `examples/transformer/meta_pipeline.py`
- 后续建议新增一个 Transformer 方法装配模块

职责：
- 构建 clean model
- 注入 layer tags
- 为 baseline / checkpoint / SAC / built-in graph remat / ATenIR strategy N 返回统一的 method handle

#### Layer 3 — capture / benchmark 执行层
位置：
- `examples/transformer/capture_flow/*`
- `examples/transformer/benchmark_flow/*`

职责：
- `capture_flow`：图语义验证、meta/runtime 对照、boundary consistency
- `benchmark_flow`：真正的 static/runtime benchmark、effectiveness check、结果汇总

#### Layer 4 — 底层算法与统计引擎层
位置：
- `aten_recompute/core/*`
- `aten_recompute/analysis/*`
- `aten_recompute/utils/*`

职责：
- graph rewrite
- strategy selection
- static simulation
- runtime profiling
- artifact / JSON / plot 输出

原则：
- 不把实验 orchestration 再塞回 `aten_recompute/core/`
- `core` 只负责算法和 backend；实验编排留在 `examples/transformer`

### 10.3 建议的未来文件位置安排

#### A. 方法矩阵定义
建议位置：
- `examples/transformer/benchmark_flow/strategy.py`

未来职责：
- 不只维护 ATenIR strategy tag
- 还统一维护 method registry，例如：
  - `eager_baseline`
  - `eager_checkpoint`
  - `eager_sac`
  - `compiled_baseline`
  - `pytorch_graph_remat`
  - `atenir_strat6`
  - `atenir_strat7`

#### B. 方法装配模块
建议新增位置：
- `examples/transformer/` 下单独新增一个方法装配文件

未来职责：
- 根据 method name 返回：
  - model / backend / wrapper
  - runtime callable
  - static capture capability
  - execution mode（eager / compiled）
  - provenance tag

#### C. 统计指标 schema
建议位置：
- 优先放在 `aten_recompute/analysis/` 或 `examples/transformer/benchmark_flow/` 下的单独模块

未来职责：
- 定义统一字段：
  - measured
  - graph_exact
  - metadata_estimated
  - formula_derived
  - calibrated
- 统一 static/runtime/compare JSON 的键名与含义

### 10.4 当前阶段建议修改的文件顺序

真正开始执行时，建议按这个顺序动文件：
1. `examples/transformer/benchmark_flow/strategy.py`
   - 明确方法矩阵与 method registry
2. `examples/transformer/benchmark_flow/runtime_phase.py`
   - 接入 PyTorch built-in baseline 与统一 runtime profiling
3. `examples/transformer/benchmark_flow/static_phase.py`
   - 强化 static provenance 和分项统计
4. `examples/transformer/meta_pipeline.py`
   - 补充通用 helper，避免逻辑散落
5. `aten_recompute/analysis/static.py`
   - 提升 static simulator 精度与分项输出
6. `aten_recompute/analysis/profiler.py`
   - 强化 runtime schema 和 static/runtime 对账

### 10.5 当前阶段不建议做的结构动作

为了避免过度重构，当前不建议：
- 改动 `aten_recompute/core/` 的总体目录布局
- 再新建一套 benchmark 框架
- 把 `examples/transformer` 的 orchestration 下沉回 core
- 因为对比 PyTorch built-in 而放弃现有 YAML 主入口

更合理的做法是：
- 保持目录稳定
- 在当前真实存在的 `examples/transformer` 框架内补齐 built-in recomputation 和高精度 static 对账能力
- 逐步让 README、主入口和代码结构重新一致

# Critical files to modify

后续真正执行时，优先会动这些文件：

- `examples/transformer/benchmark_flow/strategy.py`
  - 扩成方法矩阵与 method registry，而不只 ATenIR strategy tag
- `examples/transformer/benchmark_flow/runtime_phase.py`
  - 接入 PyTorch built-in 方法与统一 runtime profiling
- `examples/transformer/benchmark_flow/static_phase.py`
  - 区分 graph-captured static、formula-derived static、future calibrated static
- `examples/transformer/meta_pipeline.py`
  - 放通用装配/对照 helper，避免逻辑散落
- `aten_recompute/analysis/static.py`
  - 提升 static simulator 精度、分项统计和 provenance
- `aten_recompute/analysis/profiler.py`
  - 增强 runtime schema 与 static/runtime 对账输出
- `examples/transformer/main.py`
  - 保持唯一入口，只做 task 分发
- `examples/transformer/input.yaml`
  - 若需要扩展方法矩阵配置，做最小化扩展

# Existing functions/utilities to reuse

- `examples/transformer/meta_pipeline.py`
  - `inject_transformer_layer_tags()`
  - `capture_static_with_retry()`
  - `compare_capture_semantics()`
- `examples/transformer/model_loader.py`
  - `build_transformer_from_map()`
- `examples/transformer/benchmark_flow/train_phase.py`
  - 公共训练上下文构建方式
- `aten_recompute/analysis/static.py`
  - `StaticEstimator`
- `aten_recompute/analysis/profiler.py`
  - `MemoryProfiler.profile_step()`
- `aten_recompute/utils/checkpoint.py`
  - `apply_activation_checkpoint()`
- `examples/simple_mlp/capture.py`
  - `_SACBackend`
  - `partition_fn=min_cut_rematerialization_partition`
  - `create_selective_checkpoint_contexts(...)`
- `aten_recompute/utils/graph_utils.py`
  - boundary / structure compare helper

# Verification

## Structure verification
开始实现前应确认：
- 统一入口仍为 `examples/transformer/main.py`
- `capture_flow` 负责图语义验证，`benchmark_flow` 负责性能与对账
- built-in PyTorch 方法不会直接塞进 `core/`，而是通过 `examples/transformer` 层装配

## Research verification
在真正实现前，应先确认以下问题都已经回答清楚：
- PyTorch built-in recomputation 的用户入口是什么？
- `min_cut_rematerialization_partition` 在整条 PyTorch compile 链路里扮演什么角色？
- 当前 Transformer 例子里，哪些 built-in 方法还没有接入？
- dropout / RNG replay 在各条路径上的语义风险是什么？
- static 仿真当前哪些项是精确统计，哪些项仍是近似？

## Implementation verification
后续开始实现后，应验证：
- `examples/transformer/benchmark.py` 能同时跑通 region-family 与 graph-family 的对照
- 每种方法的 execution mode 被正确标记（eager / compiled）
- PyTorch built-in 方法不再只停留在 static 公式推导，而是有真实 runtime 数据
- static report 能输出更细粒度的分项统计与 static/runtime gap

## Correctness verification
在开始比较 memory/runtime 之前，必须先验证：
- forward loss 接近
- 关键参数梯度接近
- optimizer step 更新接近
- 若 dropout 开启，RNG 语义已被明确控制或明确标注为非严格 correctness 对比

## Effectiveness verification
对每类方法都要先检查：
- 相对 no-recompute 是否至少在某个目标上产生正向收益；
- 若没有收益，是否能定位到是语义不匹配、调度限制，还是统计误差；
- 不能在“相对 no-recompute 都无效”的前提下，直接讨论策略之间谁更好。

## Interpretation check
最终结论必须避免以下错误表述：
- “PyTorch min-cut 没效果”
- “partition_fn=min_cut_rematerialization_partition 就等于 PyTorch 官方完整方案”
- “checkpoint-region 与 graph-level remat 可以直接视为同一种方法”
- “static 数字低就等于真实运行更省显存”

更稳妥的目标结论应是：
- PyTorch 用户级方法在 Transformer 上是否被正确复现，以及相对 no-recompute 是否有效；
- PyTorch 图级 partitioner 在 compile-mode 下的表现如何；
- 本仓库 ATenIR strategy 7 是否至少在某些配置上真正体现了重计算技术效果；
- static simulator 对 runtime 的逼近程度如何，误差主要来自哪里。

## Suggested next execution step
开始实现之前，先基于上述结构图、方法矩阵和指标矩阵，确定一个最终的 benchmark/dataflow 方案，再进入代码阶段。

## Phased execution order
后续真正执行时，按以下顺序推进：
1. 接对并验证 PyTorch built-in recomputation；
2. 验证每条方法相对 no-recompute 的有效性；
3. 提升 static simulator 精度并做 runtime 对账；
4. 最后再进入策略优劣比较。

## Key outcome target
最终要得到的不是一组零散 benchmark，而是：
- 一个被正确复现的 PyTorch recomputation baseline；
- 一个能体现 ATenIR 路线真实效果的 benchmark harness；
- 一个尽可能精确、并经过 runtime 校准的 static simulation pipeline。

只有做到这三点，后续关于 strategy 6/7、PyTorch built-in、ATenIR min-cut 的比较才有意义。

## Immediate planning conclusion
在执行之前，应该先把“项目结构 / 文件位置 / 方法矩阵 / 指标矩阵 / benchmark dataflow”一次性定清楚；当前计划已经把这些边界和建议位置明确下来，后续实现应严格按这个结构推进。

## 11. 建议的后续详细方案（执行前的最终设计口径）

### 11.1 框架组成
- `main.py`：唯一入口
- `capture_flow`：语义验证与图分析
- `benchmark_flow`：benchmark / profiling / summary
- `analysis/*`：静态与运行时统计引擎
- `core/*`：ATenIR graph recomputation 算法
- `examples/simple_mlp/capture.py`：PyTorch built-in 接线参考

### 11.2 文件职责
- `benchmark_flow/strategy.py`：方法矩阵
- `benchmark_flow/runtime_phase.py`：方法执行与 runtime 基准
- `benchmark_flow/static_phase.py`：静态仿真与 provenance
- `meta_pipeline.py`：通用 helper
- `analysis/static.py`：高精度 static simulator 核心
- `analysis/profiler.py`：runtime ground truth 核心

### 11.3 数据流
1. YAML 输入
2. main.py 分发 task
3. benchmark_flow/capture_flow 调方法装配层
4. 方法装配层构建 baseline / built-in / ATenIR 运行对象
5. static_phase 生成静态结果
6. runtime_phase 生成 runtime 结果
7. 汇总 static/runtime gap 与相对 no-recompute 有效性
8. 输出 IR / JSON / 图表

### 11.4 核心判断标准
- 先看 correctness
- 再看相对 no-recompute 是否有效
- 再看 static/runtime gap 是否可解释
- 最后才看方法间优劣

### 11.5 当前最重要的执行前结论
后续实现应优先围绕“正确复现 PyTorch built-in + 建立高精度 static/runtime 对账框架”展开，而不是先继续堆更多一次性 benchmark。

# Current detailed scheme ready for execution
- 结构边界已明确
- 关键文件位置已明确
- 方法矩阵承载位置已明确
- static/runtime 数据流已明确
- 后续执行顺序已明确

可以在这个基础上再开始真正动代码。

# End of plan
































































































































n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n
n  - `MemoryProfiler.profile_step()`
- `aten_recompute/core/partition.py`
  - `_apply_strategy()` 已实现 strategy 6/7 调度
- `aten_recompute/core/min_cut.py`
  - `solve_min_cut_recompute(...)`
- `aten_recompute/utils/checkpoint.py`
  - 普通 PyTorch layer-checkpoint 包装器

### 目前 Transformer 里缺失 / 需要补接的
- PyTorch SAC / selective checkpoint 在 Transformer 上的真实 runtime 路径
- PyTorch built-in graph-level remat 在 Transformer 上的“方法级”接法
- 将这些 built-in 方法纳入 `examples/transformer/benchmark_flow/runtime_phase.py` 的统一 profiling

### 已有先例但只在 SimpleMLP 中存在
- `examples/simple_mlp/capture.py`
  - `_SACBackend`
  - `min_cut_rematerialization_partition`
  - `create_selective_checkpoint_contexts(...)`
  - `CheckpointPolicy`

这个文件应作为 Transformer 侧接入 PyTorch built-in recomputation 的参考模板，而不是直接拿它的结果外推到 Transformer。

## 4. 先解决“什么算公平对比”，再写代码

后续对比只允许出现以下两类公平实验：

### 实验组 A：用户级方法对比（region semantics）
同一 Transformer，同一 batch / seq / optimizer / step 定义，对比：
- eager baseline（无 checkpoint）
- eager + 普通 checkpoint
- eager + SAC
- eager + memory budget（若当前版本存在用户级可用入口）

这一组回答的是：
- PyTorch 面向用户的内置 recomputation 方法，真实效果如何

### 实验组 B：图级方法对比（graph semantics）
同一 Transformer，同一 compiled workload，对比：
- compiled baseline（无 remat）
- PyTorch built-in graph remat 路径
- ATenIR `strategy 7`

这一组回答的是：
- 在 graph-capture / compile-mode 下，PyTorch graph-level 路线和 ATenIR 路线相比如何

### 明确不能直接拿来作为主结论的对比
- “裸 `min_cut_rematerialization_partition` backend” vs ATenIR
- eager checkpoint timing vs compiled ATenIR timing
- 公式推导的 `pytorch_ckpt` static 结果 vs runtime peak 结果
- 当前 repo 中 `compiled_transformer` vs 自定义 `autograd.Function` wrapper 的 timing 差异

## 5. 先把 dropout / RNG 作为一级风险处理

调研已经确认：dropout / RNG 不是小细节，而是 correctness 前提。

### PyTorch checkpoint/SAC
- `torch.utils.checkpoint` 本身有 RNG state 保存/恢复逻辑
- selective checkpoint 的 `context_fn` 在 compile 下还有额外约束
- 在带随机性的 region 内，不能默认假设行为与普通 eager 完全等价，必须验证

### ATenIR strategy 7
- 本仓库已经在 `aten_recompute/core/min_cut.py` 里：
  - ban 随机 op
  - ban 计算密集 op
  - 修复 attention tuple/getitem sibling consistency
- 这说明项目作者已经意识到：random / attention output mixing 会直接影响梯度正确性

因此后续实验建议固定分两阶段：

#### 阶段一：语义验证阶段
- 在 dropout 关闭或 RNG 可控条件下对比
- 先验证 loss / grad / optimizer step 是否一致

#### 阶段二：真实训练阶段
- 再决定是否打开 dropout 做 memory/runtime study
- 若 repo 路线无法保证 dropout replay 语义一致，则必须在结论中明确说明

## 6. 明确 whole-graph 与 checkpoint-region 的差异，避免误用结论

后续文档和实验报告里必须明确写清：

- checkpoint / SAC：本质上是**region replay**
- `min_cut_rematerialization_partition`：本质上是**joint-graph partitioning**
- 本仓库 ATenIR strategy 7：本质上是**在 FW→BW boundary saved values 上做 graph-level selective recomputation**

所以：
- “ATenIR 比 checkpoint 更省显存” 这种话不能直接说，除非 replay scope 和比较语义已经对齐
- 更稳妥的写法应该是：
  - “在 graph-level rematerialization 语义下，ATenIR 与 PyTorch built-in partitioner 的表现对比”
  - “在 region checkpoint 语义下，PyTorch 内置方法的表现对比”

## 7. 推荐先做一张‘语义地图’，再进入实现

开始编码前，先把待比较方法整理成一个方法矩阵：

- method name
- semantic family（region / graph）
- execution mode（eager / compiled）
- user-facing API / internal API
- RNG / dropout handling
- 当前 repo 是否已接线
- 是否适合作为主结论 baseline

这个矩阵会直接决定后续 benchmark harness 怎么组织，避免继续出现“拿 internal partitioner 代表完整方法”的问题。

# Critical files to modify

后续真正实现时，优先会动这些文件：

- `examples/transformer/benchmark_flow/runtime_phase.py`
  - 把 PyTorch built-in 方法纳入统一 runtime profiling
- `examples/transformer/benchmark_flow/runner.py`
  - 保持薄封装，接统一 orchestrator
- `examples/transformer/benchmark_flow/static_phase.py`
  - 区分“图捕获静态结果”和“checkpoint 公式推导结果”，避免误读
- `examples/transformer/benchmark.py`
  - 入口保持简单
- `examples/transformer/input.yaml`
  - 若需要方法矩阵配置，可最小扩展

若需要参考现有 PyTorch built-in 接法，应重点复用：
- `examples/simple_mlp/capture.py`

# Existing functions/utilities to reuse

- `examples/transformer/meta_pipeline.py`
  - `inject_transformer_layer_tags()`
  - `capture_static_with_retry()`
- `examples/transformer/model_loader.py`
  - `build_transformer_from_map()`
- `examples/transformer/benchmark_flow/train_phase.py`
  - Transformer 训练步骤的公共组织方式
- `aten_recompute/analysis/static.py`
  - `StaticEstimator`
- `aten_recompute/analysis/profiler.py`
  - `MemoryProfiler.profile_step()`
- `aten_recompute/utils/checkpoint.py`
  - `apply_activation_checkpoint()`
- `examples/simple_mlp/capture.py`
  - `_SACBackend`
  - `partition_fn=min_cut_rematerialization_partition`
  - `create_selective_checkpoint_contexts(...)`
- site-packages PyTorch 源码（只作语义依据）
  - `torch/utils/checkpoint.py`
  - `torch/_higher_order_ops/wrap.py`
  - `torch/_dynamo/variables/higher_order_ops.py`
  - `torch/_functorch/partitioners.py`
  - `torch/_functorch/config.py`

# Verification

## Research verification
在真正实现前，应先确认以下问题都已经回答清楚：
- PyTorch built-in recomputation 的**用户入口**是什么？
- `min_cut_rematerialization_partition` 在整条 PyTorch compile 链路里扮演什么角色？
- 当前 Transformer 例子里，哪些 built-in 方法还没有接入？
- dropout / RNG replay 在各条路径上的语义风险是什么？

## Implementation verification
后续开始实现后，应验证：
- `examples/transformer/benchmark.py` 能同时跑通 region-family 与 graph-family 的对照
- 每种方法的 execution mode 被正确标记（eager / compiled）
- PyTorch built-in 方法不再只停留在 static 公式推导，而是有真实 runtime 数据

## Correctness verification
在开始比较 memory/runtime 之前，必须先验证：
- forward loss 接近
- 关键参数梯度接近
- optimizer step 更新接近
- 若 dropout 开启，RNG 语义已被明确控制或明确标注为非严格 correctness 对比

## Interpretation check
最终结论必须避免以下错误表述：
- “PyTorch min-cut 没效果”
- “partition_fn=min_cut_rematerialization_partition 就等于 PyTorch 官方完整方案”
- “checkpoint-region 与 graph-level remat 可以直接视为同一种方法”

更稳妥的目标结论应是：
- PyTorch **用户级方法**在 Transformer 上的实际效果如何；
- PyTorch **图级 partitioner**在 compile-mode 下的表现如何；
- 本仓库 ATenIR strategy 7 相比哪一类 PyTorch 方法更强、更弱，强弱具体体现在哪个语义层级上。

## Suggested next execution step
开始实现之前，先基于上述语义地图确定一个**最终方法矩阵**，再进入代码阶段。矩阵至少包含：
- eager baseline
- eager checkpoint
- eager SAC
- compiled baseline
- PyTorch built-in graph remat
- ATenIR strategy 7

只有矩阵定清楚后，后续 benchmark 结果才值得解释。